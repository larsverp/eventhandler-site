@model IEnumerable<Rockstar.Models.Event>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "_admin";
}


@*<div class="col-md-10 right-box">
    <div class="top-text">
        Lots of great things are gonna happen
    </div>
    <div class="event-table col-md-10 offset-1">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Title</th>
                    <th scope="col">Date</th>
                    <th scope="col">Tickets sold</th>
                    <th scope="col">Last updated at</th>
                    <th scope="col"><label>Update</label></th>
                    <th scope="col">Remove</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var e in Model)
                {
                    <tr>
                        <th scope="row">@e.title</th>
                        <td>@e.date</td>
                        <td>@e.seats</td>
                        <td>@e.date</td>
                        <td><a asp-action="EditEvent" asp-route-id="@e.id">Update</a></td>
                        <td><a asp-action="DeleteEvent" asp-route-id="@e.id">Delete</a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>*@

